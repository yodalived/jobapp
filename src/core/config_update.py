# Add these to your existing config.py if not present
from typing import Optional, List

# Also install python-jose with cryptography
# poetry add "python-jose[cryptography]" passlib bcrypt python-multipart
